# Rust
/target/
**/*.rs.bk

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# macOS
.DS_Store
._*

# Logs and scratch run artifacts (never commit)
*.log
/output/llm_labels.csv

# Model artifacts (push to HuggingFace, decision 0014)
# Only the `default` symlink is tracked — everything else is downloaded at
# build time via .github/scripts/download-model.sh or set via FINETYPE_MODEL_DIR.
models/**/*.bin
models/**/*.safetensors
models/**/config.json
models/**/config.yaml
models/**/label_map.json
models/**/labels.json
models/**/results.json
models/**/*.jsonl
models/**/eval/
models/*.csv
models/*.json
models/*.md
!models/.gitkeep

# YDF lens artefacts under eval/gittables/models/ — regenerable from
# scripts/train_ydf.py. The model binary directory is multi-GB and the
# training-rows manifest is ~16 MB; the small JSON/vocab files stay tracked.
eval/gittables/models/ydf.bin/
eval/gittables/models/training_rows_manifest.tsv

# Value-level YDF prior (spec 2026-06-04) — ~780 MB model dir + regenerable
# manifest/CSV, all rebuilt by scripts/value_ydf/ac01_train_prior.py. The
# pinned feature contract lives in output/value-level-labelling/.
eval/gittables/models/ydf_value/
eval/gittables/models/ydf_value_manifest.txt
output/value-level-labelling/synthetic_features.csv
# Large/real-value intermediates — regenerable from scripts/value_ydf/
# (ac02 scoring, ac04 triad). The *.parquet carry real gittables sample
# values (secret-scanner false-positive risk, as with the corpus parquets);
# quarantine_label_errors.csv likewise. Reports + memo under the same dir
# ARE tracked. *.ndjson is already covered by the global rule below.
output/value-level-labelling/scored_values.parquet
output/value-level-labelling/kept_rows.parquet
output/value-level-labelling/quarantine_label_errors.csv

# DBpedia annotations parquet (~96 MB) — regenerable from
# scripts/extract_dbpedia_annotations.py against corpus_paths.txt.
eval/gittables/corpus_pass/dbpedia_annotations.parquet

# Corpus-pass outputs (multi-GB) and run logs — regenerable from
# scripts/gittables_corpus_pass.py.
eval/gittables/corpus_pass/files.parquet
eval/gittables/corpus_pass/columns.parquet
eval/gittables/corpus_pass/columns.pre-ydf.parquet
eval/gittables/corpus_pass/execute_log.jsonl
eval/gittables/corpus_pass/execute_summary.json
eval/gittables/corpus_pass/fill_ydf_summary.json
eval/gittables/corpus_pass*.log
eval/gittables/corpus_pass*.run*.log
eval/gittables/corpus_pass.run*-*/

# ac-08 — per-column reject extract + intermediate path lists.
# Regenerable: criterion_b_failing_paths.txt from files.parquet via
# scripts/gate_summary_sql.sql variant; per_column_rejects.parquet
# from scripts/extract_per_column_rejects.py.
eval/gittables/corpus_pass/criterion_b_failing_paths.txt
eval/gittables/corpus_pass/per_column_rejects.parquet
eval/gittables/corpus_pass/per_column_rejects_errors.jsonl
eval/gittables/per_column_rejects.log

# ac-08 mechanism attribution log + parquet (regenerable).
# The parquet contains real gittables sample values; some incidentally
# match GitHub's secret-scanner patterns (e.g. 20-char uppercase
# alphanumeric strings parse as candidate AWS Access Key IDs).
# Regenerable from scripts/build_mechanism_decomposition.py.
eval/gittables/mechanism_decomposition.log
eval/gittables/corpus_pass/mechanism_decomposition.parquet

# false-veto sweep: _before/ is a regenerable pre-fix snapshot (HEAD
# copies of the gate module + label YAMLs). Only the sanitised
# report.md + veto_rate_delta.json deliverables are tracked.
output/false-veto-sweep/_before/

# ac-09 outputs (regenerable from scripts/build_corroborated_gaps.py).
# corroborated_gaps.parquet carries sample_evidence with real
# corpus values — same false-positive risk as mechanism_decomposition.
# single_lens_signals.tsv is ~240MB / 2.6M rows.
eval/gittables/corpus_pass/corroborated_gaps.parquet
eval/gittables/corpus_pass/single_lens_signals.tsv

# ac-13 reproducibility sub-pass outputs (calibrate half, 100 files).
# Regenerable via scripts/test_corpus_pass_reproducibility.sh.
# Same secret-scanner false-positive risk as the measure-half parquets.
eval/gittables/corpus_pass_calibrate_reprocheck/
eval/gittables/corpus_paths_calibrate_100.txt

# Claude scheduled-task lock file (transient)
.claude/scheduled_tasks.lock

# Generated training data
*.ndjson
!eval/datasets/**/*.ndjson
data/*
!data/cldr/
!data/label_remap.json
data/cldr/json/

# DuckDB extension build
extension/build/
*.duckdb_extension
# community-extensions build contract (extension-ci-tools) generated state
/build/
/configure/venv/
/configure/platform.txt
/configure/extension_version.txt

# Python
__pycache__/
*.pyc

# Eval outputs (generated data, not source).
# Excluded as a glob (NOT the whole directory) so individual files like
# the validate-corpus baseline + post-fix reports can be tracked
# explicitly per spec 2026-04-28-validate-precision-corpus ac-14.
eval/eval_output/*.csv
eval/eval_output/*.json
eval/eval_output/*.parquet
eval/eval_output/*.db
eval/eval_output/*.tsv
eval/eval_output/*.log
eval/eval_output/profile_results.csv
eval/eval_output/actionability_results.csv

# Legacy CSV-sidecar outputs from the pre-MADR-0064 `finetype validate`
# flow. The new flow writes a single `.db` file via --db <path>; these
# patterns exist to keep stray pre-MADR-0064 runs out of git.
eval/datasets/csv/*.csv.valid.csv
eval/datasets/csv/*.csv.invalid.csv
eval/datasets/csv/*.csv.errors.jsonl
eval/datasets/csv/*.schema.json

# Spike experiment outputs
output/spike-training/
output/spike-results/

# v20 retrain artefacts (per spec 2026-05-23-ydf-specialist-geography).
# Multi-GB corpus-pass parquets + the merged distilled training input
# are regenerable from scripts/overnight_v20_geography.sh.
output/distillation-v20/
output/corpus-pass-v20/

# v21 GeoNames-sourced retrain artefacts (per spec 2026-05-24-v21-geonames-geography).
# Regenerable from scripts/overnight_v21_geonames.sh.
# The generator config (eval/geonames/) IS tracked — those YAMLs are
# the reproducible recipe; this gitignore only excludes the bulk output.
output/distillation-v21-geonames/
output/distillation-v21/
output/corpus-pass-v21/

# v22 boundary-training retrain artefacts (per spec 2026-05-25-v22-boundary-training).
# Regenerable from scripts/overnight_v22_boundary.sh — the mining scripts
# (mine_corroborated_gaps_training.py, mine_v21_hard_negatives.py,
# generate_wikidata_person_columns.py) ARE tracked; the bulk CSVs they
# emit are not.
output/distillation-v22/
output/corpus-pass-v22/
output/branch-ablation-v22/

# v23 precision-retrain artefacts (per spec 2026-05-27-v23-precision-retrain).
# Regenerable from scripts/extract_v23_hard_negatives.py +
# scripts/build_v23_distilled.py + scripts/overnight_v23_precision.sh.
# hard_negatives.parquet (1.3 MB) IS tracked under output/v23-precision-retrain/
# — small enough and the per-spec ac-01 artefact; the larger blend csv.gz
# (23 MB) and corpus-pass parquets are not.
output/distillation-v23/
output/corpus-pass-v23/

# YDF-validation-gate parquets — regenerable from
# scripts/apply_ydf_validation_gate.py against the four corpus passes.
# ~300 MB each; the markdown reports (cell_deltas_gated.md,
# coverage_report.md, v22_re_baseline.md) ARE tracked.
output/ydf-validation-gate/*.parquet

# Distillation batch files (rolled into *_distilled.csv.gz)
output/distillation-v3/sherlock_batch_*.csv
output/distillation-v3/sherlock_batch_*.done
output/distillation-v3/distill_concat.log
output/distillation-v3/*_distilled.csv.gz

# v17: per-type v4 distilled CSVs are reproducible via loaders/*.py;
# track the loaders + SOURCES.md, not the CSVs or HTTP cache.
output/distillation-v4/*.csv
output/distillation-v4/loaders/_cache/
output/multibranch-training/

# Agent-generated temp scripts
classify_batch_*.py
write_batch_*.py

# External repos (submodules / symlinks)
duckdb-finetype/
homebrew-tap/

# Model directories (large binary artifacts)
models/tiered-v2/
# Experimental CharCNN value-level models (spec 2026-06-04, INCONCLUSIVE).
# Regenerable from scripts/value_ydf/; manifest.json escapes the models/**
# binary patterns, so exclude the whole dirs.
models/char-cnn-v13-feat/
models/char-cnn-v13-probe/
models/char-cnn-v14-feat/
models/char-cnn-v15-dirty/
models/char-cnn-v15-gittables/

# RunPod (credentials, ephemeral config)
.runpod_ssh.env
.runpod_config.env
budget.json

# Research (ephemeral outputs)
research/run.log
research/results.tsv
research/__pycache__/
research/.venv/

# Training result logs (ephemeral)
results/

# Claude Code worktrees
.claude/worktrees/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads-archive/

# CodeGraph local index (machine-local, regenerable via `codegraph index`)
.codegraph/
output/eval-ceiling-diagnosis/coord_veto_candidate.parquet

# Header-hint ablation pass outputs (large, real values; regenerable)
output/eval-ceiling-diagnosis/abl_*/

# Fossil-cleanup ac-03 (spec 2026-06-10-fossil-cleanup): keep-class dirs'
# regenerable run outputs. Verdict JSONs / reports / fixed file lists in
# the same dirs ARE tracked — these patterns exclude only the bulk.
# Corpus-honest gate: per-candidate sample passes (~33 MB each,
# real corpus values) + stderr/stdout captures. Regenerable from
# scripts/corpus_honest_gate.py + gittables_corpus_pass.py.
output/corpus-honest-gate/*_pass/
output/corpus-honest-gate/*.err
output/corpus-honest-gate/*.out
output/corpus-honest-gate/refined/*.err
# Drift-precheck stderr/runlogs (verdict JSONs + .files.txt are tracked).
output/destination-drift-precheck/*.err
output/destination-drift-precheck/*.runlog
# latdec corpus pass: mirror the corpus_pass/ parquet rules (the
# columns.parquet is the MODELS-table input, kept on disk only).
eval/gittables/corpus_pass_latdec/corpus_pass/columns.parquet
eval/gittables/corpus_pass_latdec/corpus_pass/files.parquet
eval/gittables/corpus_pass_latdec/corpus_pass/execute_log.jsonl
eval/gittables/corpus_pass_latdec/corpus_pass/execute_summary.json
# Mining-factory distilled training inputs (regenerable from
# scripts/mining_factory/manufacture.py + distillation pipeline).
output/mining-factory/*.csv.gz
# Mining-factory working CharCNN models (campaign-local, like the
# char-cnn-v13/v14/v15 experiment dirs above).
models/char-cnn-base/
models/char-cnn-mfg/
# Value-level arch-test working models (eval_*.json verdicts tracked).
output/value-level-arch-test/model_base_nf/
output/value-level-arch-test/model_mfg_nf/

# Postal-veto gate sample pass (regenerable from scripts/gittables_corpus_pass.py
# with the patched binary over stratified_sample.files.txt; verdict JSON is tracked).
output/postal-veto/sample_pass/
output/postal-veto/sample_pass.err
output/postal-veto/sample_pass.out

# v27 recall blend CSV — regenerable via scripts/build_v27_recall_distilled.py
output/v27-recall-retrain/sherlock_distilled_v27.csv.gz

# v27/R32-era sample-pass outputs — regenerable via gittables_corpus_pass.py (verdict JSONs tracked in output/corpus-honest-gate/)
output/text-vocab-override/
output/veto-fallback/

# False-veto-trio gender fix sample pass — regenerable via gittables_corpus_pass.py
# over stratified_sample.files.txt (verdict JSON tracked in output/corpus-honest-gate/).
output/false-veto-trio/sample_pass/
output/false-veto-trio/sample_pass.err
output/false-veto-trio/sample_pass.out

# Corpus-pass + snapshot byproducts under output/ (regenerable; multi-MB / transient)
output/**/sample_pass/
output/**/*.files.txt
output/**/sample_pass_hier/
output/**/*.nohup
output/cardinality-readjudication/
output/enum-domain-emission/

# Prior-session research artifacts (closed specs) — keep the tree clean
output/aba-checksum-guard/
output/binary-veto/
output/city-region-corrob/
output/corpus-honest-gate/
output/country-code-corrob/
output/determinability-probe/
output/duckdb-shellout-spike/
output/housekeeping-baseline/
output/increment-veto/
output/isbn-checksum-guard/
output/isbn-veto/
output/securities-checksum-guard/
output/url-veto/

# Representative-baseline study (this session) — regenerable dumps + local finding;
# durable record is memory representative-baseline-is-068 + card 0020 + the spec.
output/representative-baseline/

# reshape measurement intermediates (large/reproducible; conclusions are in *.md)
output/model-label-space-reshape/ac2/*.ftmb
output/model-label-space-reshape/ac2/*.tsv
output/model-label-space-reshape/ac3/*.tsv
output/model-label-space-reshape/retrain/*.log

# ── m2v dual-encoder model dirs (2026-06 potion campaign) ──
# Multi-GB weight artifacts; pushed to HuggingFace per choice 0014. Only the
# tracked models/default symlink (→ m2v8m-s43) points in; weights fetched by
# .github/scripts/download-model.sh. Explicit-dir list mirrors the
# char-cnn-v13/14/15 precedent above.
models/m2v-code16m-encoder/
models/m2v32m/
models/m2v8m/
models/m2v8m-s42/
models/m2v8m-s43/
models/m2v8m-s44/

# ── Regenerable experiment bulk under output/ (2026-06 campaigns) ──
# Same discipline as every block above: the .md reports / verdict JSONs /
# manifests in these dirs stay tracked; only the bulk below is ignored.
# Never-tracked bulk extensions (0 tracked instances — safe as globs):
output/**/*.ftmb
output/**/*.pid
output/**/*.out
# Wholesale bulk working dirs (verified to contain no durable records):
output/composed-accuracy-roadmap/incgate/
output/composed-accuracy-roadmap/tierb-gate/
output/distillation-identity/
output/identity-fortification/
# clean-label-retrain: keep VERDICT/ac0 md + manifests; drop blends + score tsvs
output/clean-label-retrain/*.csv.gz
output/clean-label-retrain/scores/*.tsv
output/clean-label-retrain/scores/*/*.tsv
# composed-accuracy-roadmap/rescore: keep report_*.md; drop preds/metrics tsv
output/composed-accuracy-roadmap/rescore/*.tsv
# dual-encoder-native: keep **/report_*.md; drop metrics/preds tsv
output/dual-encoder-native/**/*.tsv
# embed-frontier: keep ac0*_*.md; drop eval ftmb (glob above) + preds
output/embed-frontier/preds/
# gte-tiny-embed-swap: keep ac0*_result.md; drop run sentinels
output/gte-tiny-embed-swap/*.done
output/gte-tiny-embed-swap/_floortrain_done.txt
# sharpen-audit: keep ac*_*.md, score_*/report_*.md, tierA/report_*.md +
# BATCH1_RESULT.md; drop preds/metrics tsv, ablation dumps, tierA caches/binaries
output/sharpen-audit/*.tsv
output/sharpen-audit/ablation/
output/sharpen-audit/guard_ablation/
output/sharpen-audit/score_*/*.tsv
output/sharpen-audit/tierA/*.tsv
output/sharpen-audit/tierA/cache/
output/sharpen-audit/tierA/gate_batch1/
output/sharpen-audit/tierA/gate_batch2/
output/sharpen-audit/tierA/finetype_*

# Nested corpus-pass byproducts inside experiment dirs (parquets, jsonl,
# execute_summary) — regenerable from scripts/gittables_corpus_pass.py,
# same as eval/gittables/corpus_pass/. The verdict .txt/.md alongside them
# stay tracked. Mirrors the output/**/sample_pass/ rule above.
output/**/corpus_pass/

# Scratch memory dumps at repo root (knowledge already saved as orbit
# memories; kept on disk, never tracked).
/scratch_*.txt

# W2b gate baseline/candidate (large derived parquets, regenerable)
output/company-reference-audit/w3_baseline_with_oracle.parquet
output/company-reference-audit/eval_w2b_substance/candidate_4type.parquet
output/company-reference-audit/item9_baseline_with_oracle.parquet
output/company-reference-audit/eval_item9/sample_pass/
output/company-reference-audit/sexp_baseline_with_oracle.parquet
output/company-reference-audit/eval_sexp/sample_pass/

# Blanket-ignore derived experiment output + model tokenizer artifacts (regenerable) — 2026-07-19.
# Supersedes the per-experiment enumeration above; already-tracked files are unaffected.
output/
models/**/tokenizer.json
